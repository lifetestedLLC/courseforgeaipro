# Authentication
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here

# IMPORTANT: Set NEXTAUTH_URL based on your environment:
# - For LOCAL DEVELOPMENT (npm run dev): Use http://localhost:3000
# - For PRODUCTION (deployed): Use your production domain with HTTPS
# Example for local dev:
NEXTAUTH_URL=http://localhost:3000
# Example for production (uncomment and replace with your domain):
# NEXTAUTH_URL=https://courseforgeai.org

# Email Service (Optional)
# Currently in development mode (logs to console)
# To enable email sending, uncomment and configure one of these services:
# 
# Option 1: Resend (Recommended for Next.js)
# EMAIL_API_KEY=re_your_resend_api_key_here
# 
# Option 2: SendGrid
# SENDGRID_API_KEY=SG.your_sendgrid_api_key_here
# 
# Option 3: AWS SES
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1

# OpenAI API
OPENAI_API_KEY=your-openai-api-key-here

# Stripe
STRIPE_SECRET_KEY=your-stripe-secret-key-here
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret-here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key-here

# Stripe Price IDs (create these in your Stripe dashboard)
STRIPE_STARTER_PRICE_ID=price_starter_id_here
STRIPE_PROFESSIONAL_PRICE_ID=price_professional_id_here
STRIPE_ENTERPRISE_PRICE_ID=price_enterprise_id_here

# Database
# ⚠️ IMPORTANT: Different formats for local development vs production!
#
# FOR LOCAL DEVELOPMENT:
# 1. Run: npx prisma dev
# 2. Copy the DATABASE_URL from the output (starts with prisma+postgres://)
# 3. Paste it below
# Example format: DATABASE_URL="prisma+postgres://localhost:51213/?api_key=..."
#
# FOR PRODUCTION (Vercel/deployed):
# 1. Set up a PostgreSQL database (Vercel Postgres, Supabase, or Neon)
# 2. Get the connection string from your database provider
# 3. Add it to Vercel environment variables (NOT in this file!)
# Example format: postgresql://user:password@host:5432/database?sslmode=require
#
# 📖 See PRODUCTION_DATABASE_SETUP.md for detailed production setup instructions
DATABASE_URL="your-database-url-here"
